============
Installation
============

At the command line::

    $ pip install fuel_plugin_service

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv fuel_plugin_service
    $ pip install fuel_plugin_service
